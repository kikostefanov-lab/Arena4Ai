/**
 * THE competition the sizzle reel is cut from. One constant, one place.
 *
 * Deliberately NOT 78c7452f ("Arena4Ai — Commercial Launch Strategy"). That
 * competition is the only three-team run in the database, but its SUBJECT is a
 * commercial strategy the project abandoned, and the subject is what the reel
 * shows: its rubric rows are `monetization-viability` and `gtm-strategy`, and
 * its forge artifacts are `pricing-tiers.md` and `launch-checklist.md`. Those
 * are the visual in the scores and forge scenes, not background detail, so no
 * amount of re-narrating makes them incidental.
 *
 * The three-provider claim does not depend on it: GladiatorReveal renders
 * claude / codex / gemini procedurally, and the leaderboard aggregates all three
 * across every competition — both stronger evidence than a single match.
 */
export const SIZZLE_COMPETITION_ID = 'ebb45f36-250c-4084-b87f-d58bb52129b1';
