node_modules/
dist/
.env
runs/
*.local
.turbo/
*.tsbuildinfo
.next/

# OS / tooling
.DS_Store
.superpowers/
.worktrees/
.wrangler/

# Database backups
*.dump

# Local debug artifacts
.claude/
.playwright-mcp/
# Ad-hoc screenshots taken while debugging. NOTE: README.md must not embed a
# file matching these patterns — it did, and the images 404'd on GitHub for
# every visitor while looking fine locally. Images the README shows belong in
# docs/, which is tracked.
/arena-v2-*.png
/homepage-*.png

# Rendered video outputs (regenerable from sources)
packages/video/out/
